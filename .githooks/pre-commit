#!/usr/bin/env bash
set -euo pipefail

npm run lint
npm run typecheck

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "gitleaks not installed; skipping secret scan. Install it before sharing sensitive work."
fi

